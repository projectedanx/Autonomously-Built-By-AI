### DRP_ID: UI-TOPOLOGY-2026
### DESIGNATION: The Topological UI Crucible (Neo-Gothic Analytics)
### TELEOLOGY: To generate a highly complex, accessibility-compliant React 18.3.1/Tailwind 3.4.4 landing page by navigating strict non-Euclidean layout constraints and mereological boundaries.

+++ChatScope 
+++ContextLock(anchor="CORE_TECH_STACK_AND_ACCESSIBILITY", refresh_interval=2048)
+++Role(persona="Senior Tactile Epistemic Architect & Atlas Integrator")
+++EntropyAnchor(level="low", focus="structural_topology_and_causal_logic")

[PRECONDITIONS]
1. Core Technologies: React 18.3.1, TypeScript 5.5.3, Tailwind CSS 3.4.4, DOMPurify 3.2.6.
2. You must enforce the Immune-Aware Petzold Loop: THINK -> WRITE -> CODE.

[INVARIANTS: THE PDL v1.0 DECORATORS]
+++AdjectivalBound(max_per_entity=2, type_preference="limiting")
+++MereologyRoute(relation_type="Component-Object", transitivity_check=true)
+++SpatialBind(Region_X="Hero_Data_Vis", Region_Y="Navigation_Matrix", RCC8="Partial Overlap (PO)")
+++DCCDSchemaGuard(schema=React_AST_Component, enforcement="draft_conditioned")
+++AutonymicIsolate(forbidden_pattern=["eval()", "dangerouslySetInnerHTML"], treat_as="mention-of")

[EXECUTION DIRECTIVE: THE PETZOLD LOOP]

+++PetzoldSequence(phase="THINK")
- Execute +++SilentReasoning(depth="high").
- Calculate the Spatial Cadence (Greatest Common Divisor of padding/margin) for a Brutalist layout grid.
- Analyze the RCC-8 constraint: The 'Hero_Data_Vis' must Partially Overlap (PO) the 'Navigation_Matrix'. Map this 3D constraint into Tailwind z-index and negative margin properties.

+++PetzoldSequence(phase="WRITE")
- Draft the Component Mereology. Define the Atomic Design Hierarchy (Atoms, Molecules, Organisms).
- Establish the Chromatophore visual tokens [17]. The theme is "Neo-Gothic Crystalline": Obsidian facades, brushed metal ribs, bioluminescent data streams. 
- Output a pure JSON manifest detailing the Props interface and state transitions.

+++PetzoldSequence(phase="CODE")
- Synthesize the final React (TSX) code.
- Apply +++DCCDSchemaGuard. Ensure semantic HTML5 tags (<article>, <nav>, <section>) and strict WCAG 2.1 ARIA states (`aria-expanded`, `aria-live`) are present.
- Eliminate standard "AI-smooth" aesthetics. Replace vague CSS with rigid, physics-based Tailwind utility classes.

[POSTCONDITIONS]
- The generated code must pass an Abstract Syntax Tree (AST) validation.
- The RCC-8 'Partial Overlap' MUST be mathematically demonstrable in the CSSOM structure.
- Output the code inside a strict Markdown fenced block.
+++Execute(format="strict_Petzold_schema")