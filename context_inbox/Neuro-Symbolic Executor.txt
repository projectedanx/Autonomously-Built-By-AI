Build a Neuro-Symbolic Executor that combines a LLM with a symbolic mathematics solver like SymPy. When a user inputs a mathematical expression or equation, the executor should first attempt to parse and solve it using SymPy. If direct symbolic solving is not possible or if a hybrid approach is needed, use the LLM's attention mechanisms to probe the underlying structure and intent, feeding relevant information to SymPy or a numerical computation engine for execution. Ensure the system can report procedural success rates.

Create an Atomic Tokenization Module that modifies the existing BPE tokenizer. This module should identify and treat common STEM symbols (e.g., ∇, ∂, Σ, ∫, ∞, ≡, ⇒, ⇔, ∀, ∃) as single tokens, rather than splitting them into multiple sub-word units. Integrate FoNE-inspired embeddings for these atomic symbols to capture their richer semantic meaning.