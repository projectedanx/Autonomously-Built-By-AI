# KIRA-7 Agentic Feature Emergence: Implementation Plan

## Phase 1: Incorporating KIRA-7 Constraints into the Swarm
1.  **DCCDSchemaGuard Enhancement:** Update the existing `DCCDSchemaGuard` in the orchestrator to strictly validate `Feishu_Card_JSON_v2` before any JSON is emitted.
2.  **SagaRecovery Protocol:** Implement token-refresh-first (`tenant_access_token`) logic within the `SagaRecovery` protocol to preempt SCAR-001 failures.
3.  **Petzold Loop Enforcement:** Ensure the system logic (`worker_run.py`) rigorously tracks the `THINK|WRITE|CODE|IMMUNE_REVIEW` state transitions, specifically suspending KIRA-7's gritty persona during the `CODE` phase.

## Phase 2: Webhook Sovereignty & The CFDI Brake
1.  **Ingress Validation Middleware:** Implement the required Feishu verification logic (URL Verification Challenge, AES-256-CBC Decryption, SHA256 Signature Verification) as mandatory middleware for any bot deployment generated by the system.
2.  **Scope Isolation Gate (Rule 4):** Modify the context ingestion logic (`orchestrator.py` or similar) to automatically halt and request human clarification if Feishu permission scopes or event triggers are missing from the raw intent.

## Phase 3: Validation and Verification
1.  **Negative Control Tests:** Create tests that intentionally supply invalid JSON structures to verify the `DCCDSchemaGuard` intercepts the Ontological Shear.
2.  **Replay Attack Simulation:** Test the `X-Lark-Signature` verification by submitting stale timestamps to ensure rejection.
